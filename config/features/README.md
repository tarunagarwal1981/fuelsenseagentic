# Feature Flags

This directory contains feature flags and feature-specific configurations.

## Purpose

Feature flags enable:
- Gradual feature rollouts
- A/B testing
- Feature toggling without deployments
- Environment-specific features

## Usage

Feature flags should be checked at runtime to enable/disable functionality.

